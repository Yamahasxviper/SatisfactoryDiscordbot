# Place Windows libwebsockets library here

Add:
- websockets_static.lib (static library)

Or if using dynamic:
- websockets.lib (import library)
- websockets.dll (dynamic library - place in Binaries)
