; SMLWebSocket – no plugin-specific settings required.
; This plugin provides only the core WebSocket client (USMLWebSocket).
; Discord Gateway configuration has moved to the DiscordGatewayMod plugin.
